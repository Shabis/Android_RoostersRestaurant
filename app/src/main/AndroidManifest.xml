<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roostersdining.roostersrestaurant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.OrderActivity" />
        <activity android:name=".ui.BreakfastItemActivity" />
        <activity android:name=".ui.EggOptionActivity" />
        <activity android:name=".ui.ToastOptionActivity" />
        <activity android:name=".ui.MenuActivity"></activity>
    </application>

</manifest>